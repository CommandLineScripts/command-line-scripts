import { Executable } from '@clscripts/cl-common'

export interface {{pascal packageName}}Opts {
  // add your options here
} 
export class {{pascal packageName}} implements Executable {
  packageExecutable: string = "{{packageExecutable}}"
  get command(): string {
    return [this.packageExecutable].filter(Boolean).join(' ')
  }

  constructor(opts: {{pascal packageName}}Opts) {}
}
